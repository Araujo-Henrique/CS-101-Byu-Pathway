colors = "red green blue yellow"

color_parts = colors.split() # This method change the variale with many values to a list, but to do it we need to creat a new variable. In this case "colors_parts"
# Another point is that when the parenteses doesn't have nothing, automatically the values will be defined by each blank space
print(colors)
print(color_parts)


names = "Henrique,Beto,Dinha,Claudia,Flavio"

each_name = names.split(",") # Now the program will define each value by the ","
print(names)
print(each_name)